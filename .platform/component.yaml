apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: "spec-teste"
  description: "Artifacts generator via Spec."
  annotations:
    github.com/project-slug: overview-demo/spec-teste
    backstage.io/techdocs-ref: dir:..
    github.com/workflows: publish.yml,remove.yml
    kong-manager/service-name: app.spec-teste.80
    kong-manager/instance: cluster-overview-demo
    kong-manager/spec : openapi-swagger.yaml
    grafana/dashboard-selector: "spec-teste"
    grafana/alert-label-selector: "alertname=spec-teste"        
spec:
  type: devops
  lifecycle: experimental
  owner: "group:default/admin"
  providesApis:
    - spec-teste
